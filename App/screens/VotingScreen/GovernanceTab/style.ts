import {StyleSheet} from 'react-native';
import {Colors, Sizes} from 'theme';

const style: any = {
	container: {
		paddingTop: Sizes.smartHorizontalScale(20),
		paddingHorizontal: Sizes.smartHorizontalScale(37),
	},
};

export default StyleSheet.create(style);
