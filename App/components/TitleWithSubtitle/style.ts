import {StyleSheet} from 'react-native';
import {Colors} from '../../theme';

const style: any = {
	container: {
		alignItems: 'center',
		flex: 1,
	},
	title: {
		textAlign: 'center',
		color: Colors.white,
	},
	subtitle: {
		textAlign: 'center',
		color: Colors.white,
	},
};

export default StyleSheet.create(style);
